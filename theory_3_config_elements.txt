Remember:
    Config Elements - provide configuration and helper elements. Most commonly used are
        HTTP Cookie Manager - allows to manage cookie suatomatically
        HTTP Cache Manager - allows to cache elements like a real browser does
        HTTP Request Defaults - allows to specify settings in one place that are inherited down
        User Defined Variables
            Variables Defined here have global scope. Each Thread receives own copy of variable accessible by ${VAR_NAME}